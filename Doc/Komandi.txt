#ВЕСЬ ПРОЕКТ ДОЛЖЕН БЫТЬ  ЧИСТЫМ БЕЗ СТАРЫХ БИБЛИОТЕК И ТОЛЬКО PYTHON 3.10 

#для запуска окружения перед началом работы введи следующее:

    .\venv\Scripts\Activate.ps1
    (если не заходит в окружение то создать его заново 
    python -m venv venv
    повторить первый код)

#выгрузка базы(хуйня то что дерево слево, легче сразу эти 3 команды ввести и не ебаться, главное ненужные файлы указать в .gitignore)

    git add .
    git commit -m "Что сделал"
    git push origin main

#Загрузка базы 

    git pull origin main

#Загрузка нужных библиотек 

    pip install -r requirements.txt



